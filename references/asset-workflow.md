# 资产制备工作流

> **执行方说明（重要）**：本文件里的「图片生成提示词结构 / 资产SPEC」是**交付物**，不是 agent 自动执行的指令。
> - **纯提示词模式**（默认）：agent 只把这些提示词与 SPEC 交给用户，由用户去即梦/Seedance/MiniMax/GPT-Image 等平台手动生成资产。
> - **生成模式**：仅当用户已接生图能力时，agent 才实际调用生成并把文件存到下方目录。
> 严禁在纯提示词模式下指挥 agent 调用 imagegen / 生图动作。

资产数量由分镜脚本逐镜盘点决定，脚本里出现几个就做几个，不套固定默认值。只生成实际镜头需要的资产，避免超过模型素材预算。

## 资产盘点规则（按脚本，不套默认值）

Phase 8 从分镜脚本逐镜盘点，脚本需要什么就列什么，数量等于脚本实际出现的次数：

| 类型 | 数量依据 | 用途 |
|------|---------|------|
| 产品四视图（**1×4 拼接单图**） | 每 SKU 1 张 | 锁定产品外观（正/背/侧/顶四角度并排），跨视频共用 |
| 人视三视图（**1×3 拼接单图**） | 每个出镜角色 1 张 | 锁定「是谁」（正/侧/背三角度并排），跨视频共用 |
| 角色服装设定 | 每条视频按场景风格 1 套 | 锁定「穿什么」，每条视频变化 |
| 场景锚点 | 脚本出现几个场景就做几张 | 锁定空间、光线、关键物位置 |
| 关键道具 | 脚本出现几个道具就做几个 | 道具参与反转时使用 |
| 首尾帧 | 每镜 0-2 张，按脚本定 | 固定构图或跨镜衔接时使用 |

## 资产复用规则（多条视频之间）

- **人视三视图（1×3 拼接单图）：跨视频共用**。同一角色所有视频共用同一张三视图拼接单图，只生成一次，不重复生成（真人 IP 必须共用）。
- **产品四视图（1×4 拼接单图）：跨视频共用**。同一 SKU 所有视频共用同一张四视图拼接单图，只生成一次，不重复生成。
- **其它资产（服装设定 / 场景锚点 / 道具 / 首尾帧）：每条视频独立，禁止跨视频复用**。即使两条视频用「同类型场景」（比如都是办公室），也要各自重新生成不同变体，不沿用上一条的图。生成时每次产出新变体（文件名带视频编号或 `-v2`/`-v3`），不覆盖旧文件。
- **多品（多 SKU）**：每个 SKU 一张三视图拼接单图，互不混用。

## 产品四视图 / 人视三视图 制备规范（**两类都是 1×N 拼接单图**）

产品资产和角色身份锚点是唯一要求「实物/身份一致性 100%」的两类资产——**两类都按「一张图内 1×N 横排拼接」交付，绝不拆成多张独立图**。这种方式的好处：① 一次出图占 1 个资产位（不是 3-4 个），降低模型资产预算占用；② 角度连续可见，客户一眼审过；③ 喂给视频模型作参考时整图锁定，不出现角度漂移。

### 共通硬性规范

- **形态**：**1×N 横排拼接单图**（产品 1×4：正/背/侧/顶；人视 1×3：正/侧/背）。
- **画幅与风格**：白底电商风，16:9 横版（产品因 4 角度稍密，可降至 16:9 更紧凑亦可），光线自然（无戏剧光、无硬阴影），电商质感细腻真实。
- **标注**：每个角度下方加一行小字标签（如「正视图」「后视图」「侧视图」「顶视图」或人视的「正面」「侧面」「背面」），标签字号一致、居中、颜色浅灰，**不喧宾夺主**。
- **不增量原则**：产品四视图就是 4 个角度（不上 6 视图、不上下双排）；人视三视图就是 3 个角度（不上四视图）。
- **生成方式**：均做图生图——把客户提供的实拍图/真人照片作为唯一参考输入，让生图模型基于参考做拼接规整；**禁止文生图凭空画**（Logo、文字、五官比例必漂）。
- **一致性 100%**：实物/身份细节（产品轮廓/颜色/材质/Logo 方向/文字/型号/接口/开合状态；人物脸型/发型/肤色/体型/身高比例）必须与参考完全一致，不增删任何细节。
- **缺角度处理**：参考图不全时，**先问客户**——「缺失的 X 视图，是补拍给我，还是用 AI 基于现有参考补全？」。客户选 AI 补的角度，必须在资产台账里标「待客户确认」，不得当作实拍看待。
- **验收**：整张图（包含全部拼接角度）一次性交客户确认，点头才进视频；标签、外缘、整体构图有任一不适，整图重做。

### 产品四视图（每 SKU 1 张，1×4 拼接）

- 拼接顺序：正视图 → 背视图 → 侧视图 → 顶视图（左→右）。
- 4 个角度平分画幅，每个角度下方有「正视图/后视图/侧视图/顶视图」小字标签。
- 比例：单罐单瓶类 4 角度充裕；复杂产品（多 SKU 套装）可改为 2×2 拼接，但仍是单图。

### 人视三视图（每角色 1 张，1×3 拼接）

- 拼接顺序：正面 → 侧面 → 背面（左→右）。
- 3 个角度平分画幅，每个角度下方有「正面/侧面/背面」小字标签。
- 风格硬性锁定「黑白素描铅笔画」（过人脸审核，详见下方「资产视觉指令集 ② 人视三视图」）——本 SKILL 规定人物资产一律黑白素材，无任何写实豁免（视频阶段再还原真实风格）。
- 范围：以单人正面为主（脸部特写或全身均可，依角色锁定需要），姿态一致，便于锁定身份；三视图以锁定脸型/发型/体型为核心。
- 真人 IP 跨视频共用；虚构角色每条视频独立。

## 角色资产 = 身份锚点（**人视三视图 1×3 拼接单图**）+ 服装设定（拆分）

角色资产拆成两件事，分别管理：

### 身份锚点（人视三视图 1×3 单图，锁定「是谁」）

> 严格按上方「人视三视图」规范生成——一张单图内左→右为「正面 / 侧面 / 背面」，三角度并排、标签清晰、白底电商风。这是本 SKILL 唯一接受的「身份锚点」形态；不接受「一张正面 + 几张散图」。

- 锁定：年龄段、性别、脸型、肤色、五官比例、发型发色、体型、身高关系
- 风格硬性锁定黑白素描（过人脸审核），无任何写实豁免——本 SKILL 规定人物资产一律黑白素材
- **真人 IP**：跨视频共用同一张人视三视图单图（因为是同一个真实的人，共用才能保证「同一个人」）
- **虚构角色**：每条视频独立生成（每条是不同的角色设定）
- 不要把不同年龄/发型/五官塞进同一锚点；衍生图一次只改一个条件（表情/姿势/机位）
- 多人先分别生成各自的人视三视图单图，再用身高/站位区分

### 服装设定（锁定「穿什么」，每条视频变化）

- 上衣/下装/鞋/配饰、主辅色、材质
- **无论真人 IP 还是虚构角色，服装每条视频都要有变化**，不沿用上一条的服装
- 服装按「视频场景风格」来定：办公室场景穿正装、户外场景穿休闲、居家场景穿便服，先看脚本场景再定服装
- 真人 IP 换装时，保留身份锚点（脸/体型/发型不变），只换服装设定

## 场景/产品/道具

电商短视频默认竖版 9:16 交付画幅（产品四视图资产例外，为白底 16:9），资产构图与分镜留白按此预留（横版仅当平台明确要求时）。

- 场景锚点写清：入口方向、窗户方向、主光方向、桌椅产品位置、镜头运动路径；跨镜保持轴线
- 场景按脚本逐镜盘点，出现几个场景就做几张，不同视频各自独立生成
- 产品四视图 1×4 拼接单图见上方「产品四视图 / 人视三视图 制备规范」，已有实拍图优先作参考输入，不重绘精确 Logo/包装文字/型号/成分表/认证
- 人视三视图 1×3 拼接单图同上，参考图真人照片优先作唯一输入
- 没有产品图只生成"概念占位图"，不把虚构包装当最终资产

## 资产视觉指令集（PROMPT 结构）

> 所有资产图的 PROMPT 统一用「字段化指令集」：每类资产一段，用【】方头括号字段，逐字段写**可见、可测、可拍摄的物理细节**，禁止抽象审美词。每类末尾固定一段「生成要求」（摄影参数级，可直接复制去平台）。纯提示词模式下这段就是交付给用户的 PROMPT；生成模式下 agent 按此调生图。

### 全局禁用词（写任何资产 PROMPT 时一律替换）

| 禁用 | 换成 |
|---|---|
| 高级感 / 氛围感 / 大片感 / 质感拉满 / 电影级光影 / 杰作 | 可见物理细节（材质/光影/构图/明暗具体化） |
| masterpiece / best quality / cinematic lighting / movie poster | 同上，用中文物理描述 |

### ① 产品四视图（每 SKU 1 张，1×4 拼接单图）

```
【产品视觉指令集】
[产品01｜产品名称｜四视图]
[形制结构：罐体/瓶身/盒体的整体轮廓、长宽厚比例、接口/开合方式、正反面层级]
[材质细节：金属拉丝/玻璃通透/纸面印刷颗粒/塑料磨砂等物理属性，写具体]
[包装文字：品牌名、LOGO、型号、配料表、酒精度等必须逐字准确，不得虚构]
[使用痕迹：符合真实产品的磨损/划痕/贴纸状态（全新品写"全新无痕"）]
[光影表现：影棚均匀柔光，主光方向、柔光面积、接触阴影]
[画面设定：超写实产品摄影，纯白背景]
[一致性锁定：轮廓/颜色/材质/Logo方向/文字与实物 100% 一致，不增删任何细节]
[生成要求：1×4 拼接单图，正/背/侧/顶左→右并排，白底电商风，16:9，每个角度下居中浅灰小字标签，图生图（客户实拍图作唯一参考），整图一次确认，严禁拆成多张独立图、严禁 3D/动漫/插画、严禁文字水印、严禁拼接错位]
```

### ② 人视三视图（每角色 1 张，1×3 拼接单图，黑白素描锁死）

```
【角色视觉指令集】
[角色01｜角色名称｜三视图]
[核心身份：年龄段、性别、脸型、肤色、五官比例、体型、身高关系]
[发型发色：发际线、分缝、刘海、长度、发色、卷曲度]
[服装细节：上下装、鞋、配饰、主辅色、材质（按脚本场景风格定）]
[配饰细节：眼镜/首饰/帽子等（无则略）]
[一致性锁定：脸/发型/体型/身高比例与参考 100% 一致]
[生成要求：1×3 拼接单图，正/侧/背左→右并排，黑白素描铅笔画（手绘排线、纸张纹理、黑白灰调、无文字无水印），白底，16:9，图生图（客户真人照片作唯一参考），每个角度下居中浅灰小字标签，整图一次确认，严禁 3D/动漫/插画/写实、严禁拆成多张独立图、严禁文字水印]
```

> 黑白素描是人物资产过审手段（Seedance/即梦真人脸审核极严），**只用在资产图，视频 PROMPT 阶段强制还原写实真人**（见 `seedance-prompt.md`）。黑白素描完整模板（风格措辞锁死，[占位符]按角色填）：`黑白素描铅笔画风格人物肖像，[性别]，[年龄]，单人正面脸部特写，正对镜头，构图居中。手绘铅笔素描质感，黑白灰调，细腻排线，明暗层次分明，保留纸张纹理。[发型五官]，[表情]，眼神看向镜头。[服装]，背景留白浅灰。纯手绘素描风格，无文字，无水印。`

### ③ 角色服装设定（每条视频 1 套，独立变体）

```
【服装视觉指令集】
[服装01｜角色名称｜服装]
[整体造型：叠穿顺序、整体轮廓、主次色关系]
[上衣/下装/鞋：领型、袖型、腰线、下摆、鞋型]
[面料材质：纤维/织法/厚度/光泽/垂坠/折痕]
[纹样工艺：纹样、刺绣、缝线、滚边、扣件（无则略）]
[一致性锁定：与身份锚点（脸/体型/发型）一致，只换服装]
[生成要求：白底，全身正/侧/背三视图，图生图（身份锚点作参考锁脸），只换服装不换脸，每条视频独立变体，严禁换脸/体型漂移/文字水印]
```

### ④ 场景锚点（脚本出现几个做几个，每条视频独立）

```
【场景视觉指令集】
[场景01｜场景名称]
[时代地域：现代/复古/户外/室内，地域文化]
[环境细节：地面/墙面/地貌/植被/天气/雾/尘/潮气]
[空间布局：前景/中景/远景、入口、视觉焦点、主要动线、尺度参照]
[核心物件：家具/道具的数量、位置、朝向、状态]
[光影色彩：光源类型/方向/高度/软硬/色温/阴影边缘/空气散射]
[材质空气：漫反射/镜面/粗糙度/湿润度/颗粒/烟雾密度]
[一致性锁定：门窗位置/家具/主光方向/前中后景关系跨镜头连续]
[生成要求：广角镜头，全景构图，单一视角，完整空间透视，无人物无剧情动作，真实物理材质与自然光影，严禁拼图/多视图/文字水印]
```

### ⑤ 道具（脚本出现几个做几个）

```
【道具视觉指令集】
[道具01｜道具名称]
[形制结构：整体轮廓、比例、部件连接、开合方式、正反面层级]
[材质细节：金属/木/石/陶/布/皮的物理属性——拉丝/磨砂/釉裂/木纹/纤维/划痕]
[制作工艺：锻造/铸造/榫卯/髹漆/雕刻/编织/缝制/烧制]
[使用痕迹：握持点/受力点/收纳方式/年代/环境（全新品写"全新无痕"）]
[光影表现：影棚主光方向/柔光面积/轮廓光位置/接触阴影/表面反射]
[画面设定：超写实产品摄影，纯白背景]
[一致性锁定：材质/颜色/形态/初始状态]
[生成要求：影棚产品摄影，纯白背景，单一正面三分之二视角，仅一个完整道具主体，微距细节清晰，接触阴影完整，严禁人物/手部/场景/拼图/多视图/文字水印]
```

### ⑥ 首尾帧（每镜 0-2 张，按脚本定）

```
【首尾帧视觉指令集】
[帧01｜视频编号｜首帧/尾帧]
[帧构图：景别、机位、主体位置、留白]
[主体：人物/产品/场景在帧内的状态]
[光影：主光方向、色温、明暗关系]
[一致性锁定：与分镜脚本该镜描述完全一致]
[生成要求：按该镜锁定画幅（竖版 9:16 / 横版），与相邻帧无缝衔接，每镜独立变体，严禁文字水印]
```

## 写实真人脸过审（原因说明）

Seedance / 即梦对真实人脸内容审核极严，即使 AI 生成的写实真人形象也常过不了审。因此本 SKILL 硬性规定：**人物资产一律用黑白素描铅笔画过审（无写实豁免），视频 PROMPT 阶段再强制还原写实真人**。黑白素描的完整字段与 PROMPT 模板见上方「② 人视三视图」；视频还原指令见 `seedance-prompt.md` 第九节。

## 本地保存规范

```
assets/
  products/      product-<sku>-four-view.png（1×4 拼接单图）                ← 跨视频共用
  characters/    char-<id>-three-view.png（人视 1×3 拼接单图，身份锚点）     ← 真人IP共用/虚构每条独立
                 char-<id>-outfit-<scene>.png（服装设定，每条视频变体）
  scenes/        scene-01-office-anchor.png（每条视频独立，变体 -v2 -v3）
  props/         prop-01.png
  keyframes/     s03-first-frame.png
```

- 产品四视图 1×4 拼接单图放 `products/`，跨视频共用，只生成一次
- 人视三视图 1×3 拼接单图放 `characters/`，真人IP跨视频共用；虚构角色每条独立
- 其它资产按视频各自生成新变体，文件名带视频编号或 `-v2`/`-v3`，不覆盖旧文件
- 文件名小写英文+数字+连字符。目标存在时创建 `-v2`、`-v3`，不覆盖

## 资产验收

- 产品四视图 1×4 拼接单图：轮廓/颜色/材质/Logo 方向/文字/开合状态与实物 100% 一致；**整图一次性交客户确认**，客户点头才进视频
- 人视三视图 1×3 拼接单图：身份特征（脸/发型/体型/身高比例）与参考 100% 一致；**整图一次性交客户确认**
- 与产品相关资产（产品图、Logo、品牌名、店铺/包装相关）：严格确认，不自动放行
- 角色：身份锚点（脸型/发型/体型）稳定；服装设定每条视频有变化且匹配场景风格
- 场景：空间布局/光源方向/时间/关键物位置连续
- 构图：符合画幅，主体完整，留出动作和字幕空间
- 安全：无可识别真人脸参考、第三方水印、随机品牌文字
- 文件：能打开，路径存在，台账状态与实际一致

不合格时只针对一个失败点重生成，保留旧文件为版本。
